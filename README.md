# semantic-encryption
A site that uses a neural network to encrypt you messages.

# https://0alter0.github.io/semantic-encryption/

Side note, use a CORS blocker to use the site because it gets caught in CORS.
